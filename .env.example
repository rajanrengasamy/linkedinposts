# LinkedIn Post Generator - Environment Configuration
# Copy this file to .env and fill in your API keys

# =============================================================================
# CLI Mode Toggles (Subscription billing via CLI tools)
# =============================================================================
# These settings enable using your existing subscriptions (Claude Max, Gemini
# Ultra, ChatGPT Pro) instead of per-token API billing. CLI tools route through
# your consumer subscriptions for significant cost savings.

# Enable OpenCode as primary tier (default: false)
# When enabled, tries OpenCode CLI first before native CLI tools
USE_OPENCODE=false

# Enable individual CLI tools (default: true when OpenCode disabled)
# Set to false to force API-only mode for specific providers
USE_CLAUDE_CLI=true
USE_GEMINI_CLI=true
USE_CODEX_CLI=true

# =============================================================================
# CLI Tool Paths (Optional - auto-detected from PATH by default)
# =============================================================================
# Only set these if CLI tools are not in your system PATH

# OPENCODE_CLI_PATH=/path/to/opencode
# CLAUDE_CLI_PATH=/path/to/claude
# GEMINI_CLI_PATH=/path/to/gemini
# CODEX_CLI_PATH=/path/to/codex

# =============================================================================
# OpenCode Model Configuration
# =============================================================================
# Models to use when routing through OpenCode CLI

# OpenCode model for Gemini provider
# Options: gemini-3-pro-preview, gemini-2.5-pro, gemini-2.5-flash
OPENCODE_GEMINI_MODEL=gemini-3-pro-preview

# OpenCode model for OpenAI provider
# Options: gpt-5.2, gpt-5.2-codex, gpt-5.1-codex-max
OPENCODE_OPENAI_MODEL=gpt-5.2

# =============================================================================
# CLI Settings
# =============================================================================

# CLI command timeout in seconds (default: 300 = 5 minutes)
# Increase for complex reasoning tasks that may take longer
CLI_TIMEOUT_SECONDS=300

# =============================================================================
# API Keys (Required only when CLI modes disabled/unavailable)
# =============================================================================
# When CLI modes are enabled (USE_*_CLI=true), these API keys serve as fallbacks.
# If CLI tools fail or are unavailable, the system automatically falls back to API.
#
# Priority order: OpenCode CLI -> Native CLI -> Direct API
#
# Required API keys based on CLI mode settings:
# - PERPLEXITY_API_KEY: Always required (no CLI alternative)
# - GOOGLE_AI_API_KEY: Required if USE_GEMINI_CLI=false and USE_OPENCODE=false
# - OPENAI_API_KEY: Required if USE_CODEX_CLI=false and USE_OPENCODE=false
# - ANTHROPIC_API_KEY: Required if USE_CLAUDE_CLI=false
# - OPENROUTER_API_KEY: Required for KIMI 2 models (no CLI alternative)

# Perplexity API Key (for web search and validation)
# Get your key at: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# Google AI API Key (for Gemini scoring and image generation)
# Get your key at: https://aistudio.google.com/apikey
GOOGLE_AI_API_KEY=

# OpenAI API Key (for post synthesis with GPT)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API Key (for Claude synthesis)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenRouter API Key (for KIMI 2 models)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# =============================================================================
# OPTIONAL API KEYS
# =============================================================================
# Only required if using --sources linkedin or --sources x

# ScrapeCreators API Key (for LinkedIn and Twitter/X scraping)
# Get your key at: https://scrapecreators.com
# WARNING: Using social sources may violate platform ToS
SCRAPECREATORS_API_KEY=
